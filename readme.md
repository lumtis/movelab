Some experimentations with Move language.

### Commands

```bash
move build
move test
move prove
move sandbox publish
move sandbox run sources/publish_balance.move --signers 0xf -v
move sandbox run sources/mint.move --signers 0xf --args 10  -v
```
